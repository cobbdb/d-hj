var opts = {};
console.debug('shop-stats.js', 'required');
module.exports = {
    groom: opts.groom || 0,
    facility: opts.facility || 0,
    doctor: opts.doctor || 0,
    gym: opts.gym || 0,
    coach: opts.coach || 0
};
